// Window CustomEvent name used to open the global support modal from anywhere
// (footer / Recursos menu / panel links) without coupling the trigger to the
// modal's mount point. The SupportModalHost (mounted once, app-wide) listens.
export const OPEN_SUPPORT_EVENT = "contratacr:open-support";
